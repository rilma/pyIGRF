language: python

python:
  - "3.5"

os:
  - linux

notifications:
  email: false

addons:
  apt:
    packages:
      - gfortran

install:
  - pip -q install 
  - python setup.py install

script:
  - python test/test.py
